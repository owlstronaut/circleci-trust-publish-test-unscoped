// circleci-trust-publish-test-unscoped
// This package exists to validate CircleCI trusted publishing integration
// It should not be used for any production purposes

module.exports = {
  name: 'circleci-trust-publish-test-unscoped',
  purpose: 'CircleCI OIDC trusted publishing validation (unscoped)'
}
